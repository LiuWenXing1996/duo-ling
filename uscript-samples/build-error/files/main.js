// 构建失败样本：验证「尽量导入」语义 —— 导入期 esbuild 失败**仍会导入**，
// 导入报告的该条 notes 里带构建诊断，脚本停在「无产物」状态（启用时注册会被拦并给警告）。
//
// 失败原因：引用了文件树里不存在的模块。语法本身合法，不吃编辑器 / 工具链的解析。
import './does-not-exist.js'

;(function () {
  console.log('[哆灵脚本] 构建失败样本：本行正常不会被执行')
})()
