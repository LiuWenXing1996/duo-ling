# 项目约定决策

> 一句话：仍生效的架构约定 + 已否决方案表；改前先读，避免同一问题被重议一遍。

## 现状

### 仍生效架构约定

- **单端架构**：只服务浏览器扩展（Chrome MV3），不抽 `packages/shared` 双端共用层——所有代码只服务扩展。
- **UI 复用**：改 UI 先查 `src/components/` 现有实现，禁照界面重写；用 `src/lib/window-api.ts` 按 `PreloadApi` 契约桥接 `window.api`，组件零改动。
- **手写数据层逐条自检四类语义**：回退 / 守卫（空标题、非法 id、未知 op、空清单）/ 先校验后落盘 / 无变化就不做（幂等短路径）；四类各补单测。常量表（如 `providers.ts`）逐字段核对——服务商预设曾少 7 个就是这么漏的。
- **主题跟随系统**：`src/lib/theme.ts` 用 `prefers-color-scheme` 驱动 `html.dark`；两个载体 html 不写 `class="dark"`（曾固定成纯黑）；新增 UI 用主题变量不硬编码颜色。
- **仓库结构**：`srcDir:'src'` 必须（WXT 内置 `@`/`~` 别名硬编码指向它且覆盖用户 alias）；`publicDir` 显式 `src/public`（否则 `tool-bridge.js` 不进产物）；包管理用 npm，产物 `.output/`。
- **生成物优先让工具自生成**：`.wxt/`、`node_modules/` 等靠装依赖时工具自己建，不靠文档提醒人跑准备命令（详见 `lessons.md` 的 WXT 现象族）。
- **权限引导收在一处**：需用户在浏览器里开启的开关（用户脚本接口）统一由工作台「引导」标签页说明并自检；各处（脚本列表横幅 / 编辑器保存警告 / 侧边栏错误条）只给「查看开启引导」入口，不各写一套步骤——版本分支（Chrome ≥138 / <138 / Firefox）只有 `src/lib/extension-page.ts` 一份文案。**该页只放需要用户动手的项**：无需操作的实现细节（如脚本世界禁 `eval` 的限制）不写进去，用户看不懂也无从操作，写了只是噪音；这类信息应由保存时的警告、错误日志在恰当时机给出。
- **不给脚本世界配 `csp`**：脚本世界用浏览器默认的严 CSP（禁 `eval` / `new Function`）——AI 生成的脚本不可控，不额外给「执行任意字符串」的能力；注入链路自身零 eval，受影响的只有内部靠 new Function 做 codegen 的依赖库，由 `collectCspWarnings` 保存时提示、生成提示词与 `script_spec` 明令避开。

### 已否决方案

只记小决策（一句话能说清的取舍），不抄两遍。新增写全「触发问题 → 结论 → 为什么不」，重做时把该行移回正文。

| 日期 | 议题 | 结论 | 为什么不这么做 |
| --- | --- | --- | --- |
| 2026-09-14 | 用户脚本「保存为草稿」按钮 | 不做 | 论证原见一期收编提案（提案流程与归档已移除，详见 git 历史） |
| 2026-09-15 | 工具页风格统一 / 沙箱化 / UserTool 改造 | 不做 | 依赖的 UserTool 工具链路已整体移除，场景无载体 |
| 2026-09-15 | 会话与工具解耦 | 不做 | 同上；已落地部分已转正常青约定 |

## 本文档不包括什么

- 双端共用层：单端架构不需要，所有代码只服务扩展。
- 同一否决项记两处：同一事实说两遍，两处会各自过期、互相打架。
- 架构决策理由的详细论证：写在对应 notes 常青篇或本表（一个事实只有一个家）。

## 决策记录

| 决策时间 | 决策点（≤100字） | 结论（≤100字） | 依据（≤100字） |
| --- | --- | --- | --- |
| 2026-09-13 | 提炼一期落地约定 | 汇成仍生效约定表，改前先读 | 避免同一问题被重议 |
| 2026-09-18 | 否决项维护 | 当下即记、重做移正文 | 理由会随清理丢失 |
| 2026-09-18 | 权限引导落点 | 独立「引导」标签页集中承载 | 原先散落三处各一行字，说不清版本分支、重复维护 |
| 2026-09-18 | 脚本世界 CSP | 不配 `csp`，回落默认严 CSP | AI 脚本不可控，不给动态执行字符串的能力 |
