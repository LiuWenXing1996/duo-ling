# AI 生成后续路线图

> 一句话：十项未做 + 一项独立（DL.page），归三提案、定开工顺序；本图只记问题与归属不写方案全文。
> 源文档：[docs/userscript-ai-generation-next.md](../../docs/userscript-ai-generation-next.md)

## 现状

- 十项未做归三提案：① 元素拾取器（含 #6 内置分组切法 A、#10 档 1/档 3 探针）；② 生成体验完善（进度通知 / 消息排队 / regenerate / 推送实测 / 运行期反馈闭环）；③ 脚本资产流转（zip 导出 / #5 notes 档案）。
- 独立项 `DL.page`（规范稿 v2 待评审，实施排 v2 Phase 4）。
- 归属判据：一件事 = 一个能独立说清「改完了没」的变更；同文件同验收不拆，必须同时改才自洽的不许拆。
- 开工顺序建议 ① → ② → ③；zip(#4) 方案已定稿可随时插队（卡点仅 `fflate` 依赖点头）；`DL.page` 保自己节奏。
- 边界：不接管 `todo.md` 条目（只加一行链接）、不写方案全文、不承诺排期。

## 待做

- 三提案评审与实施；`DL.page` 评审。

## 不做

- SW 化 esbuild（已否）；`applyIntents` 链路（老大决策不用管）；首条 `script_spec` 形态（已落地）。

## 决策记录

| 决策时间 | 决策点 | 结论 | 依据（一句） |
| --- | --- | --- | --- |
|  | #6 内置分组归属 | 切法 A，归元素拾取器提案 | 跨提案前置本身是风险，不拆去③ |
