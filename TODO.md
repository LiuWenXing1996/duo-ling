# 待办

> 只装「谁在什么场景遇到什么问题」；**允许 ≤30 字的方案短语点方向，但不装方案设计**。
> 改动后跑 `pnpm run check:todo`（查：单条 >100 字、总字数 >6000、疑似重复）。

## 规则

1. 每条 ≤100 字，**这 100 字含末尾的方向短语**（不是「100 字问题 + 30 字方向」），方向短语本身再卡 ≤30 字；主体只写「谁在什么场景遇到什么问题」
2. **不写方案设计**——方向词点到为止，禁止展开：不写实现步骤、不写数据结构/IPC 形状、不列 UI 细节、不比较备选、不分期。想得出完整方案说明已经想清楚了，那就直接去做；方案全文写进对应代码或 `README.md` / `AGENTS.md` 里
3. 不排序、不排期
4. 进清单 = 这件事要做；没排期只表示还没轮到，不表示「也许不做」
5. **了结就直接删**：做完的、决定不做的，都直接从清单里删掉——它已了结，留着只是噪音
6. **AI 可认领，不可擅自加**：AI 可以把清单里的条目当任务直接去做；但往里加条目前仍要问过用户，落笔即等于替他决定「要做」

## 清单

- 管理页里的脚本后台静默生效，时间一久用户只剩一串开关：看不出每个脚本是干嘛的、当时为什么建；AI 生成的脚本在会话删除后更无从追溯。
- 对 AI 的回答不满意时，用户只能自己在输入框重新描述一遍要求让它重答，没有现成的「重新生成」手段，旧回答也无法撤掉。

- 生成任务进行中用户再发消息，现在直接被拒（拒绝 chat:start），体验割裂。方向：生成期间允许继续输入，并让用户选排队或会话引导。
- 脚本只能靠 zip 或链接分发，没有可浏览的目录：用户无从发现有什么脚本可用，只能等别人给链接。方向：订阅式清单 + 工作台市场页。
- offscreen 单写失败时只弹错误条，没有重试按钮，用户只能自己重来（写失败可重试 UI 提示）
- 用户在编辑器点「清依赖缓存」后再次保存构建，产物仍立刻产出，疑似仍命中旧依赖缓存（HTTP 磁盘缓存 / 清理未生效待定位），用户无法确认清理动作真的发生了。
- 油猴（Tampermonkey）脚本市场生态丰富，但项目脚本格式与之不兼容，现成脚本无法直接复用。
- 脚本运行状态现在只在页面浮窗的灵动岛里，而浮窗平时不在页面里（要从 popup 打开）——用户在扩展图标、popup 上都看不到当前页的脚本跑没跑、有没有报错。方向：考虑这两处也显示运行状态。
- 脚本连续写值会乱序落盘：同步 set/delete 都不等应答，SW 侧并发处理且 delete 读到键不存在即提前返回 —— set 紧跟 delete 可能留下旧值（矩阵探针真机摇摆复现）。方向：写命令按脚本串行化。
- 非 Chrome 用户用不上：Firefox 只到「能构建」（适配停在 sidebar_action、发布链路也不发包），装上只看到降级文案；Edge 同引擎但我们只在 Chrome 上验过。方向：把 Firefox 适配纳入交付面。
- 用户关掉某个站点的接口录制后，已录的内容没有清理入口，只能一直留在本地库里。方向：补一个按站点清记录的入口。
- 接口录制的 AI 路径（浮层卡片渲染 → 开启 → 刷新 → 读回）只验过数据通路，整条流程没在浏览器里端到端跑过，真出问题无从定位。
- 浏览器没开「Allow User Scripts」时，提示只出现在工作台脚本列表的横幅里；平时不开工作台的用户看不到，只会发现脚本一个都不生效。方向：扩展图标加 badge 提示。
- 用户删掉已启用的脚本后，目标页面刷新仍在注入它（面板显示的是已删脚本的注入面），要等 SW 冷启动对账才清净。方向：删除路径并入注册串行队列。

- 让 AI 改已有脚本后，它的收尾文案说「未启用状态，需你确认启用」，而脚本实际仍是启用态（原地更新保留了它）——用户会以为没生效、反复去启用。方向：文案与实际状态对齐。
- 脚本列表卡片的元信息区（时间戳 + 运行统计）挤在一起：统计行换行后以「·」开头、与上一行几乎贴住，整块看着像没排版。方向：理顺行距与左对齐。
- 脚本列表顶部栏：筛选「已启用」后左侧统计多出「筛选显示 N 个」，文案一长就把右侧搜索框与批量操作挤到第二行，工具栏跟着变高。方向：长文案不挤占操作区。
- 工作台用标签页装所有页面，同类页面一开多标签栏就挤：脚本编辑器、运行日志、脚本历史各占一个标签，切回哪个只能靠标签名去认。方向：移除标签页，改左侧导航 + 右侧单页显示。
- GM API 支持面漂移：`gm-grants.ts` 仍登记 `GM_audio` / `GM_getTab` / `GM_saveTab` / `GM_getTabs` / `window.onurlchange`，但 VM 运行期不注入（均 TM / GM4 标准、VM 未实现），导致 catalog / spec 过度宣称「支持」。方向：从 `gm-grants.ts` 摘掉这 5 项（或将来接 VM 之外的实现）—— 详见 docs/gm-api-gap.md 第二节。
- 生成中的回答里，「思考过程」折叠条被后续内容一路往下推：token 吐得快时用户还没点到就被推走，想展开看也点不中。方向：生成期间不推走折叠条。
- 网页浮层压在页面上会挡内容，边看网页边对话缺一个与页面并排、互不遮挡的落脚处（0.3.0-alpha.1 移除侧边栏后无人承接）。方向：加回会话侧边栏。
- 用户和 AI 聊脚本时对不上号：AI 手里只有脚本名，没有按名字反查 UUID 的手段；脚本列表、运行日志等界面也不显示 UUID。方向：界面透出 UUID 并补反查。
- 接口全走 XMLHttpRequest 的站点上，脚本读不到响应体：现有钩子只拦 fetch。方向：GM.page 补 xhrHook。
- token 用量在会话列表和消息头都是一长串裸数字，过万之后扫一眼读不出量级，也不好横向比较。方向：按量级缩写展示。
- 脚本历史只能一版一版翻只读源码，用户看不出某一版到底改了什么、也没法和当前版本对照。方向：相邻版本加差异对照。
- 会话里发过图之后，每轮请求都会把这些图重新发给模型（请求体带整段历史）：几轮下来体积与 token 都成倍涨。方向：历史图片只回传最近几轮。
- 换 pnpm 后脚本版本历史恒空：保存与 AI 落盘都成功，但历史面板一直显示 0 个版本（e2e 五条依赖它的用例已临时跳过）。方向：查 us-git 在隔离布局下的静默失败。

- e2e 首跑 5 失败（4 个 spec）：注册被 VM 拒、浮层连 SW 失败、探针未注入，均非 PR 回归。方向：真机排查 VM 集成。
- store.watch/unwatch/change 零调用点，与已删 store.watchAll 同源，自研值桥疑似整体报废。方向：核实后清死代码。
- store 读路径归属不明：ARCHITECTURE 写 duo-ling 快照、dl-port 注释说已归 VM、vm-adapter 零 dispatch，三者打架。方向：真机验读路径是否 VM 原生。
