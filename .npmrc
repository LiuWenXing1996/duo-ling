# pnpm 11 仅从 .npmrc 读取 auth 与 registry 设置
# 其他配置（nodeLinker、allowBuilds 等）请见 pnpm-workspace.yaml

# electron 二进制下载走 npmmirror（GitHub 直连在部分网络环境不可用）
electron_mirror=https://npmmirror.com/mirrors/electron/
