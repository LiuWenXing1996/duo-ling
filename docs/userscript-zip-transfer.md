# 用户脚本 · zip 导入导出方案（分享语义）

> 状态：方案定稿 v1（2026-09-15 与老大讨论收敛，**待实施**）。
> 实施排队在 AI 生成主线（车道 A）合回之后——导入需要新增协议命令，冲突面与主线重合。
> 替代 [userscript-v2-plan.md](./userscript-v2-plan.md) §Phase 3 第 5 条的「待方案」状态。

## 1. 目标与非目标

**目标**：脚本分享闭环——把脚本打包成 zip 给别人，对方一键导入。v1 只做**分享**语义。

**非目标（明确后置 / 不做）**：

| 项 | 处置 |
| --- | --- |
| 备份/迁移（含 `DL.store` 运行数据） | 后置。zip 格式预留 `data/` 目录位，v1 恒空；将来做备份时以导出开关（「包含脚本数据」）形式补。理由：分享是主场景且无隐私顾虑，掺入 store 值后 zip 会携带用户浏览痕迹类数据，分享时反而危险 |
| 油猴 `==UserScript==` 格式兼容 | 不做。粘贴安装已随旧管理器删除（2026-09-15），zip 是自有格式 |
| 多选批量导出 | 后置（checkbox 态要改列表组件）。v1 = 每行导出 + 全部导出 |
| 内容指纹去重 | 不拦截。重复导入同一 zip = 两个独立副本（uuid 重生成），报告提示即可（§5.6） |

## 2. 依赖

**fflate**（~8KB、零依赖、同步 API，比 jszip 小一个量级）——本方案唯一新增依赖，**待老大点头**（未批）。

## 3. zip 格式 v1（导入导出共用，一次定死）

```
<脚本名>/                      # 目录名仅展示用，去重时加 -2 后缀；真名以 project.json 为准
  project.json                 # 见下
  files/
    main.js                    # 真实文件树展开：路径 = 项目相对路径
    lib/util.ts
  data/                        # v1 恒空，备份语义预留位（§1）
...（多脚本 = 多个平级目录）
```

**`project.json` 字段**：`v: 1`（schema 版本）、`name`、`config`（`ScriptConfig` 原样）、`entry`、`exportedAt`（导出时间）、`exporter`（`duoling/<扩展版本>`，排障用）。

**明确不进 zip 的**：`bundle`（派生物，导入侧现场构建——导入即校验，见 §5.3）、`uuid`（本机标识，导入方重生成）、`enabled`（导入恒为 false，见 §5.5）、`createdAt/updatedAt`（导入方以导入时间为准）。

**files 展开成真实文件而非内嵌 json 的理由**：人可读；对方不用哆灵也能直接看/改 zip 里的源码，改完还能导回——「分享」的语义才成立。

## 4. 导出

- **入口**：列表页（`UserscriptListPanel`）每行菜单加「导出」+ header 加「全部导出」。单脚本导出 = 只含该脚本目录的 zip，文件名用脚本名；
- **范围**：只列活跃脚本，`deprecated` 旧 GM 记录不导出（格式不同、不可编辑，有损转换不值得）；
- **隐私提示**：导出确认处固定一句文案——「导出内容包含脚本源码明文，请注意其中是否有凭据」。files 是明文，用户很可能把 token / 内网地址写死在脚本里（本项目自己的隐私红线，产品里要有同样的意识）；
- **实现面**：ui-client 现成的 `getProject` / `list` 只读方法即可，**不新增协议命令**；编解码放独立纯函数模块（`src/lib/userscripts/zip-transfer.ts`，探针脚本放 `tmp/`）。

## 5. 导入

### 5.1 入口

列表页「导入」按钮（file picker）+ 拖拽 zip 到列表区（后者可选增强）。

### 5.2 解析安全（必须）

- **zip slip 防护**：文件路径必须相对，禁止 `..` 段、盘符、绝对路径；
- `files/` 之外的顶层条目**忽略不报错**（容错）；
- `project.json.v > 1` → 该脚本跳过，报告提示「由新版哆灵导出，请先升级扩展」；`v` 缺失或 files 非法 → 跳过带原因；
- **目录名 ≠ 真名**：一律以 `project.json.name` 为准。

### 5.3 构建必做（两道关卡的第一道）

zip 不带 bundle，导入侧**必须在 offscreen 内现场构建**才能落盘——`importProjects` 与 `createProject` 同构：先构建再落盘，构建失败（语法错、TS 报错、远程依赖拉不到）该脚本跳过不落库，报告给 esbuild 的 `文件:行:列`。

这同时是导入校验：zip 是外部数据，`project.json` 长得对不代表能跑；构建是确定性验证器，把「拷贝时少了依赖文件」的残缺 zip 当场拦住。注意：远程依赖源码已物化进 files（`builder.ts` 的 dl-vfs 插件），**导入含 CDN 依赖的 zip 不需要网络**。

### 5.4 matches 提前校验

导入时就校验 match pattern 合法性，非法的归入「构建失败」同类跳过并说明哪条规则不合法——否则非法值要拖到「启用」那刻才以 `registerError` 冒出，隔了一层。校验逻辑与启用路径抽出来共用。

### 5.5 三个已定默认值

| 项 | 默认 | 理由 |
| --- | --- | --- |
| uuid | **重新生成** | uuid 是本机标识；保留原值则两台机器互导第二次必撞 uuid |
| 重名 | `nextScriptName` 自动补号 | 零交互，与新建同一套逻辑 |
| enabled | **false（停用）** | 导入脚本是任意来源代码；落盘 → 用户审源码 → 手动启用。与 AI 生成「先落盘不启用」同一产品原则 |

**两道关卡的完整链路**：选 zip → 解析校验 → 逐脚本构建（失败者出局）→ 落盘（停用态）→ 用户审源码 → 手动启用 → 注册注入。「启用」动作本身**永不触发构建**（启停零构建是既有设计），产物恒来自导入时那次构建。

### 5.6 重复导入

不做拦截。导入报告对内容指纹（hash files + entry）与现有脚本一致的条目标一句「与现有脚本「X」内容相同（仍已导入）」——提示但不替用户做决定（故意要一份副本改着玩是合理场景）。

### 5.7 失败语义

**逐脚本独立**：某个脚本构建失败只跳过它自己，成功的照常落盘（守住「先构建后落盘」不变量），最后汇总「成功 N 个，失败 M 个 + 原因」。不做整包回滚——跨记录事务做得到但没必要。

### 5.8 成功动线

- 单脚本 zip（最常见的分享形态）→ 导入成功后**直接打开该脚本的编辑器标签页**；
- 多脚本 zip → 停在列表页 + 汇总报告，新导入的标「刚导入 · 未启用」。

## 6. 协议与文件面

- **新增命令对**：`userscript:import`（扩展页 → SW）+ `state:import`（SW → offscreen），`project-write.importProjects` 落地（先构建后落盘 + git 快照，与 `createProject` 同构）；
- **导出零新增协议**（只读走现成命令）；
- **冲突面**：`extension-ipc.ts` / `ui-client.ts` / `background.ts` / `project-write.ts` 全在 AI 生成主线（车道 A）的独占地盘里 → **实施排队等主线合回**。

## 7. 实施清单（按依赖顺序）

1. fflate 依赖（待点头）；
2. `zip-transfer.ts`：zip ↔ 中间形态的纯函数编解码（含 zip slip 防护、schema 校验），探针脚本放 `tmp/`；
3. 导出 UI（每行 + 全部导出 + 隐私提示）；
4. `state:import` / `userscript:import` 命令对 + `importProjects`（含 matches 校验共用化）；
5. 导入 UI（入口 + 汇总报告 + 成功动线）；
6. 手测：含远程依赖的 zip 断网导入、恶意路径 zip、TS 构建失败 zip、重复导入。
