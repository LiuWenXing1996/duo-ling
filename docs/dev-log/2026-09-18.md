# 2026-09-18

## 工作台首页概览面板落地（修复空壳，docs/inbox.md 第 40 条）

**依据**：inbox 待办「主页标签自工具链移除后为空壳、首页放什么没定论」。2026-09-14 工具链移除后主页长期 `<div class="h-full" />` 空壳；本次围绕「用户脚本」定方案（不复用旧工具网格）：概览卡 + 快捷操作 + 最近脚本 + 空状态引导。

**落地内容**：
- 新增 `src/components/WorkspaceHomePanel.vue`：概览卡（脚本总数 / 已启用 / 最近编辑 / 健康度）+ 快捷操作（添加脚本 / 导入 zip / 让 AI 生成）+ 最近脚本（top 5，点击进编辑器）+ 空状态引导 + 引擎状态横幅。数据复用 `userscriptClient.list/errors/availability`（与列表页同源，零新增协议）；添加 / 导入动线镜像 `UserscriptListPanel`；「让 AI 生成」经 `chrome.sidePanel.open` 唤起侧边栏（API 缺失降级为轻提示）。
- `WorkspaceHost.vue`：用 `<workspace-home-panel @edit="openUserscriptEditor" />` 替换空 div；清理「内容待定 / 刻意留空」过期注释（`WorkspaceHost.vue` / `WorkbenchApp.vue`）。
- 组件单测 `WorkspaceHomePanel.component.test.ts`（4 例：概览渲染 / 空状态 / 最近点击 emit edit / 添加脚本 emit edit）。

**验证**：typecheck + build + 4 例组件单测全过。

**未做**：侧边栏唤起在无头 E2E 下不可测（需 user gesture），转手测；首页未接入 AI 生成「带页面上下文」深链（本期只打开侧边栏）。

## 首页导入报告遗漏修复

首页 `WorkspaceHomePanel.vue` 的 `onImportFile` 初版把 `importZip` 返回的 `ImportReport` 直接丢弃、只 `refresh()`，与列表页「导入后统一弹汇总报告」行为不一致。已对齐：补 `importReport` ref + `ui-dialog` 汇总报告（成功/失败 + 原因 + 指纹重复提示 + 未导入文件），与 `UserscriptListPanel.vue` 同源同套。

**教训**：镜像列表页动线时，凡列表页有「报告/反馈」类后置 UI 的，首页也必须带，不能只搬核心流程。

**验证**：typecheck + build + 原有 4 例组件单测全过（报告弹窗的手测覆盖见手测清单第 5 项）。

**补**：首页「最近编辑」列表项也补「刚导入 · 未启用」徽标（初版漏了，列表页经 `justImported` 呈现），与列表页同源同套——再次印证「镜像列表页动线须带报告/徽标类后置 UI」。

## 首页脚本删除后「最近编辑」不刷新（跨面板同步缺失）

**根因**：首页 `scripts` 是挂载时取的一次快照，只在 `onCreate`/`onImportFile` 主动 `refresh()`；脚本在列表页 / 编辑器 / 侧边栏被删后，主页常驻组件无感知、不刷新，已删脚本仍挂在「最近编辑」。

**修复**：`WorkspaceHomePanel.vue` 在 `onMounted` 注册 `chrome.storage.onChanged` 监听，过滤 `us:gm:`（删除即清该键，background `clearGMValues` 行为）与 `us:errors` 前缀变更 → 调 `refresh()` 重拉列表；`onUnmounted` 注销。加 `typeof chrome` 守卫保证测试/happy-dom 安全。无需改 background 基础设施，HMR 即生效。

**回归测试**：`WorkspaceHomePanel.component.test.ts` 新增用例——注入 `chrome.storage.onChanged` 捕获回调，模拟列表页删除 `us:gm:u1` 后断言 `list()` 被再次调用、最近编辑记录从 2 条降为 1 条。组件单测 5/5 通过。

**教训**：常驻面板的数据若只在自身动作时刷新，必须订阅外部变更源（本项目 = chrome.storage.onChanged 的 `us:gm:*`/`us:errors`），否则跨面板操作会留下过期快照。

## 决策变更：移除【最近编辑】入口（废弃跨面板同步方案）

**背景**：上一段用 `chrome.storage.onChanged` 监听做跨面板同步，用户手测仍报「列表页删除脚本后，最近编辑没清除」。

**根因（比上次更深，两处叠加）**：
1. `WorkspaceHost.vue` 的 shadcn Tabs 设 `:unmount-on-hide="false"`——隐藏的标签页内容**不卸载**，主页组件常驻，`onMounted` 只在首次挂载跑一次，切回主页不重新拉数据；
2. background 的 `clearGMValues` 删除脚本时只 `remove` 那个 `us:gm:<uuid>` 键；而默认停用的导入脚本**本就没写过该键**，`remove` 对不存在的键不触发 `onChanged` → 监听收不到 → 不刷新。即 storage 监听对默认停用脚本的删除天然失灵，是假同步。

**决策（用户拍板）**：不纠结跨面板实时同步，直接移除「最近编辑」列表入口（唯一强依赖 per-script 实时数据的区块，删除滞后最刺眼）。概览卡保留（统计数字偶发滞后可接受，不涉及具体脚本行的清除）。

**改动**（仅 `WorkspaceHomePanel.vue`，+ 删死文件）：
- 移除「最近编辑」区块（`v-if="hasScripts"` 列表 + 点击 emit edit）；空状态从「最近编辑的 else」改为独立 `v-if="!hasScripts"` 区块；
- 移除 `justImported` ref 及 `onImportFile` 里的赋值、「刚导入 · 未启用」徽标逻辑；
- 移除 `chrome.storage.onChanged` 监听（`onStorageChange` + `onMounted`/`onUnmounted` 注册注销）；`onMounted` 只剩 `void refresh()`；
- 删除未接线的死文件 `src/lib/userscripts/change-bus.ts`（本准备做页面内事件总线，决策后不再需要）；
- `WorkspaceHost.vue` 第 210 行过期注释改为「添加脚本经 @edit 开编辑器」；
- 组件测试：删两条依赖最近列表/同步机制的用例，补「有脚本不显空状态」「导入合法 zip 调 importZip」两条，共 5 例。

**验证**：typecheck ✓ / build ✓ / 组件单测 5/5 ✓。

**教训（修订上一段）**：上一段「常驻面板必须订阅 storage.onChanged」结论不严谨——storage 键不存在时监听失灵，且本项目隐藏 tab 不卸载使问题更隐蔽。per-script 实时列表类需求，要么用页面内事件总线（同源多面板共享 JS 上下文）显式广播写操作，要么不放常驻面板、由路由级挂载；寄望 storage 语义同步不可靠。本期选「移除入口」规避。

## 决策变更：移除「主页」标签，默认落脚本列表

**背景**：删掉「最近编辑」入口后概览面板仍显多余，用户决定不要独立首页——直接移除「主页」标签，默认标签页（脚本列表）无脚本时给空状态。

**改动**：
- `WorkspaceHost.vue`：删 `HOME_TAB`，新增 `LIST_TAB_ID` / `DEFAULT_TAB`（脚本列表）；`openTabs` 默认 `[脚本列表]`、`activeTabId` 默认该 id；`closeTab` 的不可关闭保护由 home 改到脚本列表；`ui-tabs` 的 `:default-value` 同步；删除 `<workspace-home-panel>` 分支，并把原链首 `v-else-if="tab.kind === 'settings'"` 改为 `v-if`；传给 `WorkspaceTabs` 的 `home-tab-id` 改名 `pinned-tab-id`。
- `WorkspaceTabs.vue`：删 Home 图标分支与 `UiHome` import；关闭按钮条件由 `tab.kind !== 'home'` 改为 `tab.id !== props.pinnedTabId`。
- `src/shared/types.ts`：`WorkspaceTabKind` 移除 `'home'`。
- 删死代码 `src/components/WorkspaceHomePanel.vue` + `WorkspaceHomePanel.component.test.ts`。
- 空状态无需新增：`UserscriptListPanel.vue` 无脚本时本就有「还没有用户脚本。可点上方「添加脚本」新建，也可在侧边栏让 AI 生成。」

**验证**：typecheck ✓ / build ✓ / 单测 326/326 ✓（删组件测试后全量复跑）。

**教训**：删掉 `v-if` / `v-else-if` 链的首个分支后，必须把下一个 `v-else-if` 改成 `v-if`——`vue-tsc` 的 typecheck **不报**模板链条错误，只有 `npm run build`（vite/rolldown 编译）会抓。这也是「交付前 build 不可省」的又一实例。

**inbox**：该待办按新规则「已办直接删」（规则 6）从「待办」移除——问题是被直接修复的，未走提案、也非「不办」。
