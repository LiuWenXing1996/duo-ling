# 元素拾取器 · 页面上下文增强（提案 ①）

> 状态：评审中
> 来源：[AI 生成用户脚本 · 后续功能路线图](../userscript-ai-generation-next.md) #1 / #6 / #10
> 提案人：

## 问题

AI 生成用户脚本的质量上限卡在页面上下文：目前只有档 0（当前标签 URL / 标题），用户说「把这个按钮改成红色」时，AI 不知道按钮在哪——选择器全靠从 URL 猜，命中率低。三个具体缺口：

1. **元素拾取器（档 2）缺失**：用户在页面上指哪，AI 应该拿到哪块元素的真实信息（结构、属性、可用的选择器），现在拿不到。
2. **页面结构信息缺失**：除了 URL / 标题，AI 对页面本身一无所知——用户不点选元素时，AI 连站点结构都看不到。
3. **内置脚本没有承载位**：拾取器这类随扩展分发的脚本，没有自己的「内置」分组，将来会混进用户脚本列表。

## 方案

### 注入通道：常驻休眠的内置 userScript

拾取器必须在**已加载的页面**上立即生效，而 `chrome.userScripts.register` 只在新导航时注入。两条可行路：

- **选定：常驻休眠**。拾取器作为内置脚本常驻注册（`*://*/*`、`allFrames: false`、固定 id、**独立世界 `us-builtin-picker`**——注册前先 `configureWorld(worldId)`，复用 `registerScript` 现成辅助函数；自定义世界不继承默认世界配置，漏配则无 `chrome.runtime`），随每次页面加载注入——但只挂一个消息监听器就休眠，页面零可见痕迹。侧边栏点「拾取」时经 `chrome.tabs.sendMessage(tabId)` 唤醒它，收到消息才亮出拾取框。**manifest 零改动**。世界选型上排除 MAIN：它唯一收益是页面 JS 全局（拾取器零需求），代价是没有 `chrome.runtime`、通信只能 postMessage 中继且可被页面伪造。
- 落选：`chrome.scripting.executeScript`（用时注入、用完即走）。注入足迹干净，但要新增 `scripting` 权限；老大拍板免权限优先，详见备选方案。

**代价（选定时一并认下）**：

1. 拾取器代码常驻用户访问的每个页面（休眠态：一个监听器，性能可忽略，但「哆灵自己的代码也在所有站点注入」——一期「生成物默认零影响」原则只覆盖生成物，内置件是显式例外，随内置分组公开可见）。
2. **版本残留**：已打开页面里躺的是加载时刻的代码，扩展更新拾取器后旧页面要等下次导航才换新。接受。
3. **唤醒通道待插桩验证**：`chrome.tabs.sendMessage` 能否投递进 USER_SCRIPT 世界，动工前先做探针确认（进验收标准）。
4. 兜底：页面里没有休眠拾取器（扩展安装前就已打开的页面、引擎未授权）时，唤醒无应答，侧边栏提示「刷新页面后重试」——不再走「注册 + 强制刷新」的老路。

### 回传通道：不经 DL 桥

路线图曾把本案标注为「唯一改动 DL 桥契约的一项」——按本方案这个前提**不成立**，在此修正：

- 拾取器是**扩展自有代码**（不是用户脚本），在 USER_SCRIPT 世界里 `chrome.runtime.sendMessage` 上报，由 SW 的**独立监听器**接收（与 DL 桥监听并存、各认各的信封标记），SW 经 `chrome.runtime` 转发回侧边栏——沿用既有「USER_SCRIPT 世界只有 runtime 消息可达」的成熟路径。扩展页直接监听拿不到 userScript 世界消息，必须经 SW 中转。
- **身份校验**：SW 监听器核对 `sender.userScript.scriptId === 拾取器固定 id`（DL 桥已有的校验模式），防其它脚本伪造信封标记冒充拾取器上报。
- DL 桥契约（`api-contract.ts` / `dl-bridge.ts` / 包装层）一行不动：拾取器消息用独立信封标记、独立监听器，不进 DL 协议。

### 拾取器交互与载荷形态

**交互**：

1. 侧边栏输入区两个动作，走同一条 `chrome.tabs.sendMessage(tabId, 唤醒消息)` 通道：**「点选元素」**（拾取模式，页面里休眠的拾取器亮出拾取态）与**「附上页面快照」**（快照模式，静默抓渲染后 outerHTML，无任何 UI）。无应答则提示刷新页面（见注入通道兜底）。
2. 拾取模式下：鼠标悬停高亮元素框（样式带命名空间前缀，不污染页面）；点击选中（事件在 capture 阶段拦截并 `stopPropagation`，页面自身不响应）；Esc / 右键取消。
3. 拾取结果与页面快照都以 chip 形式显示在输入框上方（「已点选：〈文本摘要 / 标签〉」/「已附页面快照」），随**下一条消息**作为档 2 / 快照上下文发出；可点 × 清除，可重新触发。一次拾取一个元素、至多附一份快照。
4. 面板关闭期间完成拾取 / 快照的结果直接丢弃（都是即时手势，可接受）。

**载荷形态：摘要进提示词，全量按需读**。拾取器一次性采集**超集快照**回传，消费分两层：

| 层 | 载体 | 内容 |
| --- | --- | --- |
| 摘要层 | system prompt（随 chat:start 常驻） | `tag` / `id` / `classes`、白名单关键属性（`name` / `type` / `placeholder` / `aria-label` / 截断 `href`）、≤3 条候选选择器 × **命中数**、文本样本 ~200 字符、HTML 截断 ~500 字符；整层 ≤2KB |
| 全量层 | 新 agent 工具 `element_read`（与 `script_spec` / `script_read` / `script_apply` 并列） | 拾取时的完整快照：全部属性、完整 outerHTML（宽松上限 ~32KB，防极端节点）、可选 parent 链。AI 对摘要层有不确定时**自行调用**，不用不调——token 只在实际读取时花 |

两条要点：

- **同类计数留在摘要层**：AI 自证选择器唯一性不需要额外读一次，命中数必须常驻可见。
- **「按需读」读的是拾取那一刻的快照，不是活页面**。AI 没有回查活 DOM 的通道，页面也随时可能变化——拾取时用户所指的那一块，以快照为准。全量层 = 快照的完整部分，不是实时查询。

### 页面快照（原档 1）：同一唤醒通道的第二种模式

原设想的「扩展页 `fetch` 目标 URL 拿源码」**弃用**：源码对 SPA 无效，且与渲染后 DOM 有差异时会误导选择器；而 MV3 下监听页面网络请求拿响应体是死路（`webRequest` 拿不到 body），自动附源码还让每条请求背上固定 token 开销。改为：

- **「附上页面快照」动作**：与「点选元素」共用同一个唤醒通道（`chrome.tabs.sendMessage`），快照模式**不亮任何 UI**，静默抓取渲染后的 `documentElement.outerHTML`，截断 ~32KB 经 SW 回传，显示为输入框上方 chip，随下一条消息进 `pageContext`。
- **快照即渲染后 DOM**——AI 拿到的就是选择器实际要跑的那个 DOM，服务端渲染内容也在其中（渲染 DOM 是源码的超集），SPA 同样覆盖，无源码误导问题。
- **两个动作都是用户显式点击**：页面内容只在用户说「给」的时候才离开页面，隐私语义与档 2 完全一致，不需要档 3 式的「自动读取需显式告知」设计。
- offscreen `buildSystemPrompt` 组装时标注「渲染后页面快照（截断）」。

### 档 3：本案不做

自动全页 DOM 摘要探针成本与告知复杂度都高（把整页结构送模型，落地前必须显式告知用户），且拾取器已覆盖「用户所指的那一块」。留待后续提案，见「不做的事」。

### 内置脚本承载：不进状态库，「内置」分组只读渲染

状态库写入**只归 offscreen** 是设计红线，SW 或扩展页种子写入内置项目会破单写方契约。绕开：

- 拾取器源码随扩展包构建（产物代码随包分发），由 SW 在启动 / 恢复流程里以**固定 id** 直接 `chrome.userScripts.register`（幂等），**不作为 `ScriptProject` 进状态库**。
- 管理页新增「内置」分区：从扩展包内置清单（构建期生成）只读渲染，展示名称 / 说明，**无编辑、无删除、无启用开关**——内置脚本不存在「未启用」状态。
- **一处必须处理的坑**：`registerAllEnabled` 现在是「getScripts → 全部注销 → 重注册已启用项目」的清扫式写法，会把内置拾取器一并注销。要改成按 id 排除内置脚本再清扫（进验收标准）。
- 未来再添内置脚本（如 DL.page 中继的辅助件）只需往清单加条目，分组与注册位已就位。

### 改动面

- 拾取器本体：新模块（源码 + 构建配置），含休眠监听 / 高亮 / 拦截 / 载荷采集
- SW：内置拾取器注册（固定 id、幂等）+ 拾取消息独立监听器与转发；`registerAllEnabled` 清扫排除内置 id
- `src/shared/extension-ipc.ts` + `src/shared/types.ts`：`pageContext` 扩展可选字段（档 1 HTML、档 2 元素摘要 + 全量快照）；唤醒与回传消息类型
- `src/lib/extension-chat-transport.ts`：`collectPageContext` 扩展档 2 / 快照随带（不再 fetch 源码）
- `src/lib/offscreen-chat/chat-host.ts`：`buildSystemPrompt` 组装新档位（档 2 只进摘要层）
- `src/lib/offscreen-chat/script-tools.ts`：新增 `element_read` 工具（读拾取快照全量层）
- 侧边栏 `ChatPanel`：拾取按钮 + 已点选 chip + 无应答兜底提示
- 管理页列表：内置分组（只读）
- `wxt.config.ts`：**零改动**（不新增任何权限）

## 备选方案

| 方案 | 为什么不选 |
| --- | --- |
| `chrome.scripting.executeScript`（原首选） | 注入足迹最干净（用时注入、用完即走，页面平时无哆灵代码），但 manifest 要新增 `scripting` 权限。老大 2026-09-17 拍板：免权限优先于零注入足迹，常驻休眠的代价（全站常驻 + 版本残留）认下 |
| 走 DL 桥新增命令（如 `DL.bridge.send`）回传 | 要动 `api-contract` / `dl-bridge` / 包装层三处契约，还解决不了「已加载页面注入」——绕一圈回到注入通道问题；拾取器根本不是用户脚本，不该进用户脚本的消息协议 |
| 采集时定死 ≤4KB 组合包（原方案） | 截断发生在采集时刻，AI 无法回调——截断恰好切走关键结构时就只能猜。改为「摘要 + 按需读」后，截断只影响 token 成本、不影响信息完备性 |
| 只回传选择器字符串 | 载荷最小，但复杂页面一个选择器说不清位置，AI 仍要猜；丢掉了消歧所需的属性与结构 |
| 只回传局部 HTML 片段（不发选择器） | AI 看得到结构但要自己写选择器、无从验证唯一性；同类计数是让 AI 自证的关键，不能省 |
| 内置项目种子写进状态库（管理页数据源统一） | 破「写只归 offscreen」红线，要么破例要么把种子逻辑塞进 offscreen 启动路径，两个都比只读渲染贵；内置脚本没有用户可改的状态，进库没有收益 |
| 档 1 = 每条请求自动 `fetch` 源码（原案） | 源码对 SPA 无效，且与渲染后 DOM 有差异时会误导选择器；MV3 下监听页面网络请求拿响应体是死路（`webRequest` 拿不到 body）、`chrome.debugger` 有调试横幅、hook 页面 fetch 要进 MAIN 世界且只抓得到注入后的新请求。渲染后快照是源码的超集（SSR 内容也在 DOM 里），fetch 无存留价值 |
| 档 3 自动 DOM 摘要探针一并做 | 与拾取器同属「页面上下文」但验收独立、需显式告知 UI，塞进本案会让验收清单失去「逐条可勾」性质 |

## 验收标准

- [ ] **前置探针**：`chrome.tabs.sendMessage` 能投递进 USER_SCRIPT 世界（真机插桩确认，不通则本提案打回重议注入通道）
- [ ] 侧边栏点「点选元素」→ 已加载页面立即出现高亮拾取态，无需刷新页面；休眠期间页面零可见 DOM / 样式
- [ ] 页面里无休眠拾取器时（引擎未授权等），侧边栏出现「刷新页面后重试」兜底提示
- [ ] 点击选中后面板出现 chip；Esc / 右键可取消；页面自身在拾取期间不响应点击
- [ ] 摘要层（≤2KB）随生成请求进 system prompt：选择器候选（≤3 条）× 命中数 + 关键属性 + 截断 HTML / 文本（单测覆盖 prompt 组装）
- [ ] 全量快照随 `pageContext` 到达 offscreen（≤32KB 上限），`element_read` 工具可拉全量属性 / outerHTML / parent 链（单测覆盖工具）
- [ ] 「附上页面快照」：静默抓渲染后 outerHTML 截断 ~32KB 显示为 chip，随下一条消息进上下文；prompt 标注「渲染后页面快照（截断）」（单测覆盖 prompt 组装；SPA 页面同样可用）
- [ ] 管理页出现「内置」只读分组，展示拾取器，无编辑 / 删除 / 启用控件
- [ ] `registerAllEnabled` 清扫后内置拾取器仍处于注册状态（不被「全部注销」误删）
- [ ] DL 桥契约文件（`api-contract.ts` / `dl-bridge.ts`）零改动；`wxt.config.ts` 零改动
- [ ] `npm run typecheck` + `npm run build` + `npm run test` 全过

## 不做的事

- **档 3 自动 DOM 摘要探针**：留后续提案；落地前需显式告知用户的 UI 设计另议
- **页面内容不自动附带**：拾取与快照都只在用户显式点击时采集，不做「每条生成请求自动附页面信息」
- **DL 桥契约任何改动**：拾取器不走 DL 协议
- **iframe 内元素拾取**：本期只拾主 frame，`allFrames` 场景后置
- **多元素连续拾取 / 框选**：一次一个元素
- **后台自动改脚本**：既有闸不变，拾取器只是给 AI 更好的输入，生成的脚本仍默认未启用
- **接管路线图 ② ③ 的任何条目**：进度通知 / 消息排队 / regenerate / zip / notes 均不在本案

## 决策记录

| 日期 | 决策点 | 结论 | 依据（为什么这么定） |
| --- | --- | --- | --- |
| 2026-09-17 | 注入通道 | 定 `chrome.scripting.executeScript`（新增 `scripting` 权限，评审确认项） | `userScripts.register` 只对新导航注入，碰不到已加载页面；拾取必须即时生效，注册 + 刷新的 UX 不可接受。权限代价显式摆上评审桌 |
| 2026-09-17 | 回传通道 | 不经 DL 桥，ISOLATED 世界 `runtime.sendMessage` 直达扩展页 | 拾取器是扩展自有代码不是用户脚本；修正路线图「本案唯一改 DL 桥契约」的论据——前提不成立 |
| 2026-09-17 | 载荷形态 | 五候选不单选，组合包（选择器候选 × 同类计数 + 关键属性 + 截断 HTML / 文本），≤4KB | 单项信息各有盲区；同类计数让 AI 自证选择器唯一性，是消歧关键 |
| 2026-09-17 | 档 1 / 档 3 | 档 1 接（零成本增强）；档 3 不做、留后续 | 档 1 一条 fetch 的成本换 SSR 页真实结构；档 3 需显式告知 UI，验收面独立，塞进本案破坏「逐条可勾」 |
| 2026-09-17 | 内置承载 | 内置脚本**不进状态库**，管理页「内置」分组从扩展包清单只读渲染 | 状态库写只归 offscreen 是红线；内置脚本无用户可改状态，进库无收益 |
| 2026-09-17 | 注入通道（改判） | **改为常驻休眠的内置 userScript**，推翻同日早前「`scripting` 权限」的结论；落选方案留档备选方案 | 老大提出休眠变体消掉了「注册 + 刷新」的硬伤后二选一拍板：免权限优先于零注入足迹；全站常驻与版本残留的代价一并认下。「userScripts 注入只在新导航」的机制事实不变，变的是用「常驻 + 休眠」绕开它 |
| 2026-09-17 | 回传通道（修正） | 细化：USER_SCRIPT 世界 `runtime.sendMessage` → SW 独立监听器 → `chrome.runtime` 转发侧边栏；修正同日早前行里「ISOLATED 世界直达扩展页」的表述（注入通道改判后拾取器在 USER_SCRIPT 世界，扩展页收不到 userScript 世界的消息，必须 SW 中转） | 通道归属不变（不经 DL 桥、契约零改动），只是中转路径按新注入通道落地；旧行按「只增不减」保留 |
| 2026-09-17 | 载荷形态（改判） | 摘要进 system prompt + `element_read` 工具按需读全量快照，推翻同日早前「采集时定死 ≤4KB 组合包」 | 老大指出截断该发生在消费侧而非采集侧：一次拾取回传全量，AI 按需读取。采纳——拾取器采集超集快照，摘要层 ≤2KB 常驻提示词（同类计数在内），全量层走新 agent 工具；「按需读」读的是拾取时快照而非活页面（无回查活 DOM 的通道） |
| 2026-09-17 | 档 1 改判 | 「fetch 源码」改为**「附上页面快照」**：同一唤醒通道的快照模式，静默抓渲染后 outerHTML 截断 ~32KB，显式点击触发，不自动附带 | 老大追问「fetch 之外有无其他拿源码手段」引出关键洞察：拾取器既已常驻页面，读渲染后 DOM 是白捡的能力且质量严格更优——渲染 DOM 是源码超集（SPA 可用、无源码误导）；MV3 下监听网络响应体是死路，fetch 无存留价值。显式触发保住隐私语义，不踩档 3 的告知设计 |
| 2026-09-17 | 世界归属 | 拾取器注册在**独立世界 `us-builtin-picker`**（老大开的额外决策点），排除 MAIN 世界 | 需求面：拾取器只要 DOM（USER_SCRIPT 世界全量可见）+ 消息通道（只有 USER_SCRIPT 世界有）；MAIN 世界的独门能力（页面 JS 全局）零需求，且无 `chrome.runtime`、postMessage 中继可被页面伪造、代码对页面可见。独立世界 vs 默认世界：与「每脚本一世界」惯例一致、未来内置件互不打架，代价仅是一次 `configureWorld`（复用现成辅助函数） |

## 流转记录

| 日期 | 从 → 到 | 理由（一句） | 关联 PR / Issue |
| --- | --- | --- | --- |
| 2026-09-17 | 新提案 → 草稿 | 提案创建 | — |
| 2026-09-17 | 草稿 → 评审中 | 六项决策（注入通道 / 回传通道 / 载荷形态 / 页面快照 / 内置承载 / 世界归属）逐条过审定稿，提交评审 | — |
